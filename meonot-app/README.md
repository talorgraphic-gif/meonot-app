# תוכנית מתנות למעונות

מערכת ניהול שנתית. הנתונים נשמרים בקובץ בשרת, כדי שיהיו זהים בכל מכשיר שנכנס לאותו קישור.

## הרצה במחשב שלך (רשות, לבדיקה)
```
npm install
npm start
```
ואז פותחים בדפדפן: http://localhost:3000

## העלאה ל-Railway
ראו את ההוראות שקיבלת בצ'אט, שלב אחר שלב.
